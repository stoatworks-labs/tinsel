# Files in demo/ that document or check the demo but must not be served.
# Wrangler uploads everything in the assets directory otherwise — including the
# shader checker, which would end up on a public URL.
README.md
tools/
